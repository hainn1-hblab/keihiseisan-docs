# Screenshots — Màn Template Meisai (current state)

**Cap từ**: <Môi trường dev Cổng 300>
**Ngày cap**: <2026-06-04>
**Người cap**: <DucNA1>
**User role login để cap**: <SUPER_ADMIN (6)>
**Tenant code**: <00001>
**Browser**: <Chrome 120>
**Resolution**: <vd: 1920x1080>

## Mô tả từng ảnh

### 01_meisai_template_list.png
Màn list này chỉ user role 5,6 mới vào được, mặc định khi mới load thì search all. Hiển thị tất cả template meisai của user đang login.

### 02_modal_create_meisai_ryoshusho.png
khi 領収書登録用 được chọn(hiển thị màu xanh) -> click vào button 新規登録 -> modal được hiển thị lên: tạo meisai template cho meisai hóa đơn(ryoshusho).

### 03_data_modal_create_meisai_ryoshusho.png
data mẫu được điền trong modal. khi ấn 保存 thì sẽ hiện modal confirm submit tạo mới


### 04_screen_create_meisai_ryoshusho.png
màn hình tạo meisai ryoshusho khi click vào button テンプレートとして保存 -> hiển thị modal confirm submit

### 05_modal_confirm_create.png
nếu chọn 1 template meisai ở 明細テンプレート và nhập tên mới vào 明細テンプレート名 -> sẽ hiểu là update template đó, nếu không chọn template nào ở 明細テンプレート thì sẽ hiểu là tạo mới template. Sau khi ấn 保存 thì sẽ hiện modal confirm submit tạo mới hoặc update tùy trường hợp.